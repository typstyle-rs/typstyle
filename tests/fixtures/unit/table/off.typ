// @typstyle off
#grid(  )

#table(
  // @typstyle off
  table.header(  )[#(  )],
  // @typstyle off
  table.cell(  )[#(  )],
  // @typstyle off
  table.footer(  )[#(  )],
)

#grid(
  // @typstyle off
  grid.header(  )[#(  )],
  // @typstyle off
  grid.cell(  )[#(  )],
  // @typstyle off
  grid.footer(  )[#(  )],
)
